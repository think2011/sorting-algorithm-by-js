// 希尔排序
const {swap, data} = require('./swap')

console.time('耗时')

function sort(arr) {


    return arr
}

function directInsertionSort(arr, gap = 1) {
    let len   = arr.length
    let index = null
    let cur   = null

    for (let i = gap; i < len; i++) {
        index = i - gap

    }
}

console.log(sort(data))
console.timeEnd('耗时')
